[pytest]
markers =
    semantic: supported user, author, administrator, or operator behavior
    contract: documented API, configuration, and command interface contracts
    integration: database, cache, container, proxy, and host integration behavior
    unit: isolated implementation behavior with controlled collaborators
    multinode: behavior that must also run against the multinode topology
