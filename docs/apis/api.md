In this directory we're going to explain what each endpoint in the dojo means.

Note that we're only going to take a look at the endpoints under `dojo_plugin/api/v1`.

All endpoints start with this path:

`https://pwn.college/pwncollege_api/v1`
